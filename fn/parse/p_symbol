# p_symbol — parse string as a token (skip trailing whitespace)

function p_symbol {
    typeset -n _r=$1 _s=$2
    typeset _ref=$1 _sref=$2 _str=$3
    p_string "$_ref" "$_sref" "$_str"
    if [[ ${_r.status} == err ]]; then return 0; fi
    _p_skip_spaces "$_sref"
    _r.ok "$_str"
}
