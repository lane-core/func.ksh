# p_eof — succeed only at end of input

function p_eof {
    typeset -n _r=$1 _s=$2
    if (( _s.pos >= _s.len )); then
        _r.ok ""
    else
        _r.err "expected end of input at position ${_s.pos}" $P_ERR_EXPECT "${_s.pos}"
    fi
}
