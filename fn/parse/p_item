# p_item — consume one character

function p_item {
    typeset -n _r=$1 _s=$2
    if (( _s.pos >= _s.len )); then
        _r.err "unexpected end of input at position ${_s.pos}" $P_ERR_EOF "${_s.pos}"
        return 0
    fi
    _r.ok "${_s.input:_s.pos:1}"
    (( _s.pos++ ))
}
